"use strict"; // treat all JS code as newer version

// alert(3+3) // it will work when embede inside html file and runned or on developer console but not here as JS engine is in browser
// we are using nodejs , not browser

let name = "shan";
let asge = 18;
let isloggedin = false;

// number => 2^53
// bigint
// string => ""
// boolean => true/false
// null => standalone value ()   (representationj of empty value)
// undefined => value not yet defined
// symbol => uniqueness define karne ke liye

console.log(typeof undefined); // undefined
console.log(typeof null); // object
